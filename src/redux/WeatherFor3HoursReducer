const SET_WEATHER_CHELYABINSK = 'SET_WEATHER_CHELYABINSK';
const SET_TEST_WEATHER_CHELYABINSK = 'SET_TEST_WEATHER_CHELYABINSK'


let initialState =  {
    weather: [],
    testWeather: []
};

const weatherChelyabinskReducer = (state = initialState, action) => {
    switch (action.type) {



        case SET_TEST_WEATHER_CHELYABINSK: {
            return {
                ...state,  ...action.testWeather
            }
        }
        default:
            return state;
    }

}

export const setWeatherChelyabinsk = (weather) => ({type: SET_WEATHER_CHELYABINSK, weather})
export const setTestWeatherChelyabinsk = (testWeather) => ({type: SET_TEST_WEATHER_CHELYABINSK, testWeather})


// export const getWeatherChelyabinsk = (lat) => (dispatch) => {
//     weatherAPI.profile(lat).then(response => {
//             dispatch(setWeatherChelyabinsk(response.data))
//         })
//
// }



export default weatherChelyabinskReducer;
